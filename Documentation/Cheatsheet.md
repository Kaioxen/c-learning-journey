# CHEATSHEET C 
## Input/Output
printf("text %d", var);    // print integer
scanf("%d", &var);         // read integer

## Loop Pattern
for(init; condition; update) { }
while(condition) { }
do { } while(condition);

## Common Patterns
Swap: temp = a; a = b; b = temp;
Min: min = (a < b) ? a : b;
